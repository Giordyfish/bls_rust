// use bls::{Serialize, PrivateKey};
// use bls_signatures as bls;
use bls;



fn main() {
    
    // // TEST private key gen
    // let pvkey = bls::PrivateKey::new("askdlmaksmcokijqewnaonsjcfbwasoinciasujbceiaocbeocnawi");
    // println!("{:?}", pvkey);
    
    // // TEST private key to bytes
    // let key = pvkey.as_bytes();
    // println!("{:?}", key);

    // // TEST private key from bytes
    // let keyslice = key.as_slice();
    // let pvkey2 : PrivateKey = Serialize::from_bytes(keyslice).unwrap();
    // println!("{:?}", pvkey2);
    
    // assert_eq!(pvkey, pvkey2);

    // // ############################

    // // TEST public key

}
